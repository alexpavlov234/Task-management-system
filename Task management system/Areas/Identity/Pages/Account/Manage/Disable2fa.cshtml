@page
@model Disable2faModel
@{
    ViewData["Title"] = "Disable two-factor authentication (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Това действие деактивира само 2FA.</strong>
    </p>
    <p>
        Деактивирането на 2FA не променя ключовете, използвани в приложенията за удостоверяване. Ако искате да промените ключа, 
        използван в приложението за удостоверяване, трябва да<a asp-page="./ResetAuthenticator">нулиране на ключовете за удостоверяване.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Деактивиране на 2FA</button>
    </form>
</div>
